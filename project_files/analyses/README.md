# Analysis

---

this should be where we perform hyperparameter searches, define transformer model, and do some plotting

refer to <code>comp433_braintotext/project_files/transformer/dataset.py</code> to see how they processed the data